name: notion-flowstate-bridge
version: 1.3.0
description: "Approval-gated Notion task updates, FlowState activation, and proven cross-source inventory counts."
author: local
kind: standalone
requires_env:
  - NOTION_TOKEN
provides_tools:
  - notion_data_source_schema
  - notion_data_source_list
  - notion_page_get
  - notion_mutation
  - notion_flowstate_activate
  - task_inventory_reconcile
